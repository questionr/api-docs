# <%= title %>

## Get All <%= title %>

```shell
curl "https://questionr.com/api/v1/<%= name %>" \
  -H "Authorization: Questionr secret_id secret"
```

```http
GET /api/v1/<%= name %> HTTP/1.1
Authorization: Questionr secret_id secret
Accept: application/vnd.questionr.com; version=1,application/json
Host: questionr.com
```

> The above command returns JSON structured like this:

```http
HTTP/1.1 200 OK
Content-Type: application/json
```
```json
<%= all_json %>
```

This endpoint retrieves all <%= name %>.


Parameter | Description
--------- | -----------
ids | The ids of the <%= name %> to retrieve e.g. add a query string urlencoded `?ids[]=711&ids[]=712`

> Below is a similar request with ids in a query string

```shell
curl "https://questionr.com/api/v1/<%= name %>?ids%5B%5D=711&ids%5B%5D=712" \
  -H "Authorization: Questionr secret_id secret"
```
```http
GET /api/v1/<%= name %>?ids%5B%5D=711&ids%5B%5D=712 HTTP/1.1
Authorization: Questionr secret_id secret
Accept: application/vnd.questionr.com; version=1,application/json
Host: questionr.com
```

## Get a Specific <%= title.singularize %>

```shell
curl "https://questionr.com/api/v1/<%= name %>/2" \
  -H "Authorization: Questionr secret_id secret"
```

```http
GET /api/v1/<%= name %>/<ID> HTTP/1.1
Authorization: Questionr secret_id secret
Accept: application/vnd.questionr.com; version=1,application/json
Host: questionr.com
```

> The above command returns JSON structured like this:

```http
HTTP/1.1 200 OK
Content-Type: application/json
```
```json
<%= one_json %>
```

This endpoint retrieves a specific <%= name.singularize %>.

### URL Parameters

Parameter | Description
--------- | -----------
ID | The ID of the <%= name.singularize %> to retrieve

<% if show_create_update_delete? %>

## Create <%= a_or_an(title.singularize) %>

<%= custom_str 'create' %>

```shell
curl "https://questionr.com/api/v1/<%= name %>" \
  --request POST \
  -H "Authorization: Questionr secret_id secret" \
  --data "$JSON"
```

```http
POST /api/v1/<%= name %> HTTP/1.1
Authorization: Questionr secret_id secret
Accept: application/vnd.questionr.com; version=1,application/json
Host: questionr.com
```
```json
<%= post_json %>
```

> The above command returns JSON of the newly created <%= name.singularize %>

```http
HTTP/1.1 201 Created
Content-Type: application/json
```
```json
<%= created_json %>
```

## Update a Specific <%= title.singularize %>

<%= custom_str 'update' %>

```shell
curl "https:questionr.com/api/v1/<%= name %>/2" \
  --request PATCH \
  -H "Authorization: Questionr secret_id secret" \
  --data "$JSON"
```
```http
PATCH /api/v1/<%= name %>/<ID> HTTP/1.1
Authorization: Questionr secret_id secret
Accept: application/vnd.questionr.com; version=1,application/json
Host: questionr.com
```
```json
<%= patch_json %>
```

> The above command returns JSON of the updated <%= name.singularize %>

```http
HTTP/1.1 200 OK
Content-Type: application/json
```
```json
<%= updated_json %>
```


## Delete a Specific <%= title.singularize %>

<%= custom_str 'delete' %>

```shell
curl "https:questionr.com/api/v1/<%= name %>/2" \
  --request DELETE \
  -H "Authorization: Questionr secret_id secret"
```

```http
DELETE /api/v1/<%= name %>/<ID> HTTP/1.1
Authorization: Questionr secret_id secret
Accept: application/vnd.questionr.com; version=1,application/json
Host: questionr.com
```

> The above command returns a empty JSON hash:

```http
HTTP/1.1 200 OK
Content-Type: application/json
```
```json
  {}
```
<% end %>
